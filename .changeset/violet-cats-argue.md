---
"@wso2is/admin.applications.v1": patch
"@wso2is/console": patch
---

Respect the `makeSubjectMandatory` config when persisting the subject claim's mandatory flag, instead of always forcing it to mandatory regardless of the config value
