---
"@wso2is/identity-apps-core": patch
---

Validate the `callback` request parameter of the flow execution page against the tenant's configured callback allow list before it is used as the sign in destination.
