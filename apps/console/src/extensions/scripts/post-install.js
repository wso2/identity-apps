/*
 * Node script for node package post installs.
 * 
 * If you want to add any post installs, add them here.
 * 
 * @example
 * ```
 * const { execSync } = require("child_process");
 * 
 * execSync("npm install <package_name>");
 * ```
 */
